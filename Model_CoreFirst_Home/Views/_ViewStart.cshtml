@using Microsoft.AspNetCore.Http

@{
    Layout = "_UserLayout";

    if (Context.Request.RouteValues["controller"]?.ToString() == "BooksManage")
    {
        Layout = "_Layout";

        if (Context.Session.GetString("Login") == null)
        {
            Context.Response.Redirect("/Login/Index");
        }

    }
}
